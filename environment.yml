# conda environment for repo-review (dev + CI tooling)
# install: conda env create -f environment.yml
#
# provisions node via conda-forge; every other dev/CI tool is self-contained
# (see README "Development" - no npm dependencies, no node_modules)
name: repo-review
channels:
  - conda-forge
dependencies:
  # node is the only prerequisite; the checkers and tests are self-contained
  # (no npm deps, no python)
  - nodejs>=20
